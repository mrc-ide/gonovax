## Automatically generated by odin - do not edit
.model <- odin:::odin_c_class("model", list(get_internal = "model_get_internal", finalise = "model_finalise", create = "model_create", initmod_desolve = "model_initmod_desolve", contents = "model_contents", set_user = "model_set_user", metadata = "model_metadata", initial_conditions = "model_initial_conditions", rhs = "model_rhs", rhs_dde = "model_rhs_dde", rhs_desolve = "model_rhs_desolve", output = "model_output_dde", rhs_r = "model_rhs_r"), c("A0", "beta", "eff", "enr", "epsilon", "eta", "exr", "I0", "mu", "nu", "p", "psi", "q", "rho", "S0", "sigma", "T0", "U0", "vax_t", "vax_y", "vd", "ve", "vs", "w", "n_vax"), list(discrete = FALSE, has_array = TRUE, has_output = TRUE, has_user = TRUE, has_delay = FALSE, has_interpolate = TRUE, has_stochastic = FALSE, has_include = FALSE, initial_time_dependent = FALSE), "gonovax", "odin/model.json", TRUE)
model <- structure(function (A0, beta, eff, enr, epsilon, eta, exr, I0,
    mu, nu, p, psi, q, rho, S0, sigma, T0, U0, vax_t, vax_y,
    vd, ve, vs, w, n_vax = NULL, user = list(A0 = A0, beta = beta,
        eff = eff, enr = enr, epsilon = epsilon, eta = eta, exr = exr,
        I0 = I0, mu = mu, nu = nu, p = p, psi = psi, q = q, rho = rho,
        S0 = S0, sigma = sigma, T0 = T0, U0 = U0, vax_t = vax_t,
        vax_y = vax_y, vd = vd, ve = ve, vs = vs, w = w, n_vax = n_vax),
    unused_user_action = NULL, use_dde = FALSE)
{
    .model$new(user, unused_user_action, use_dde)
}, ir = "odin/model.json", class = "odin_generator")
class(model) <- "odin_generator"
attr(model, "ir") <- .model$public_fields$ir
